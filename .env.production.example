# Production deploy — copy thành .env.production hoặc set trên Vercel/Netlify khi build
# npm run build tự load (mode production)

VITE_API_BASE_URL=https://api.cosmate.site
VITE_APP_BASE_URL=https://cosmate.site
